<% content_for :title, "New Image" %>

<div class="container mx-auto px-4 py-8 max-w-2xl">
  <div class="mb-6">
    <%= link_to "← Back to Images", images_path, class: "text-blue-400 hover:text-blue-300 transition" %>
  </div>

  <div class="glass-card rounded-lg p-6">
    <h1 class="text-2xl font-bold text-white mb-6">New Image</h1>
    <%= render "form", image: @image %>
  </div>
</div>