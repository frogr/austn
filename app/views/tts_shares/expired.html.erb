<% content_for :title do %>Audio Expired - austn.net<% end %>

<div class="container mx-auto px-4 py-8">
  <div class="max-w-2xl mx-auto text-center">
    <h1 class="font-black mb-4" style="font-size: clamp(2rem, 4vw, 3rem); color: var(--text-primary); letter-spacing: -0.03em;">
      Audio Expired
    </h1>
    <p class="text-gray-400 mb-8">
      This shared audio has expired or doesn't exist. Shared TTS audio links are only valid for 7 days.
    </p>
    <a href="<%= new_tts_path %>" class="inline-block font-semibold py-3 px-6 rounded transition duration-200" style="background: linear-gradient(45deg, #10b981, #059669); color: #fff;">
      Create New TTS Audio
    </a>
  </div>
</div>
